1.java.lang.IllegalStateException: Ambiguous mapping. Cannot map 'XXXController' method是什么错误，该怎么解决？

答：这个错误说的是"映射冲突了"。意思是你有两个Controller方法，用了相同的URL路径和HTTP方法，Spring不知道该调用哪个，就报这个错。

举个例子，你写了两个方法都映射到 GET /user/list，Spring就懵了。

怎么解决：去检查你的Controller，看看是不是有重复的@RequestMapping、@GetMapping、@PostMapping之类的路径。
找到重复的那个，改个路径就行了。另外如果用了@RequestMapping，记得把method属性也写上，别光写个路径。

---

2.@Lazy注解的作用，原理是什么？

答：@Lazy就是"延迟加载"。正常情况下，Spring容器启动的时候会把所有的Bean都创建好，但加了@Lazy之后，这个Bean不会在启动时创建，
而是等到你第一次用到它的时候才去创建。

什么时候用：比如有些Bean初始化很耗资源（连数据库、加载大文件），但你启动时不一定用到它，就可以加@Lazy，省启动时间。

原理：Spring启动时扫描到@Lazy，就不会立即实例化这个Bean，而是先生成一个代理对象（JDK动态代理或者CGLIB代理）。
等你真正调用这个Bean的方法时，代理才会去触发真实的Bean创建。

---

3.BigDecimal、float与double的区别？

答：一句话：涉及到钱的计算，必须用BigDecimal。

float和double是浮点数，它们存储的时候是近似值，不是精确值。比如0.1 + 0.2，用double算出来不是0.3，而是0.30000000000000004，
这种误差在金额计算里是不能接受的。

BigDecimal是精确计算，它把数字拆成整数部分和小数部分分别存储，不会有精度丢失的问题。

区别总结：
- float：32位，精度约6-7位小数，速度快
- double：64位，精度约15-16位小数，速度快
- BigDecimal：任意精度，没有误差，但性能比float和double慢

面试加分：说一句"阿里编码规范里明确规定，任何货币相关的字段都不能用float和double"。

---

4.springboot常见注解有哪些？

答：按用途分几个大类：

启动相关：
- @SpringBootApplication：启动类上的，相当于@Configuration + @EnableAutoConfiguration + @ComponentScan
- @Configuration：声明这是个配置类

Bean管理：
- @Component：通用组件
- @Service：业务层
- @Repository：数据访问层
- @Controller / @RestController：控制器层
- @Autowired：自动注入
- @Bean：在配置类里手动注册Bean

Web开发：
- @RequestMapping / @GetMapping / @PostMapping：路由映射
- @RequestBody：接收JSON参数
- @PathVariable：接收路径参数
- @RequestParam：接收查询参数
- @ResponseBody：返回JSON

配置相关：
- @Value：读取配置文件的值
- @ConfigurationProperties：绑定配置文件到对象
- @ConditionalOnProperty：根据配置决定是否加载Bean

AOP和事务：
- @Transactional：事务管理
- @Aspect：切面

---

5.springboot为什么不用配置tomcat？

答：因为Spring Boot内置了Tomcat。它用的是"嵌入式Servlet容器"的思路。

具体原理：Spring Boot在启动的时候，会自动把Tomcat的jar包（或者Jetty、Undertow，看你引了哪个依赖）打包进去，
然后在代码里直接把Tomcat实例化出来、启动起来，端口默认8080。

你不用像以前Spring MVC那样，先装个Tomcat，再打war包，再丢到Tomcat的webapps里。Spring Boot直接打成jar包，java -jar就能跑。

如果你不想用内嵌的Tomcat，想换成Jetty或者Undertow，就把tomcat的starter排除掉，换成别的就行。

---

6.@PostConstruct vs @PreDestroy区别？

答：这两个是Bean生命周期的钩子方法。

@ PostConstruct：在Bean的依赖注入完成之后、初始化方法之前执行。相当于你构造完对象、Spring帮你把@Autowired的依赖都注入好了，
 然后才调这个方法。通常用来做初始化操作，比如加载缓存、建立连接。

@PreDestroy：在Bean销毁之前执行。通常用来做资源释放，比如关闭连接、保存状态。

执行顺序：
构造函数 -> 依赖注入 -> @PostConstruct -> 业务使用 -> @PreDestroy -> 销毁

简单记：@PostConstruct是"出生后"，@PreDestroy是"临死前"。
